review 进度：上次 review 结尾 commit id `b52d678`

## 当前剩余问题

### [待决策] AstrBot 工具循环中的 `sticker.consider` 执行失败

- 发现范围：2026-08-04 19:13:31Z 至 19:43:31Z 的运行窗口，以及同步重启后的 10 分钟复核窗口。
- 证据：前一窗口出现 2 条 Traceback、1 条 `execution_error`、3 条 image 失败；调用栈落在 AstrBot `tool_loop_agent_runner`、`astr_agent_tool_exec` 和 `tasks`，操作名为 `yebot_sticker_consider`，异常类型出现过一次 `TypeError` 和一次泛化 `Exception`。当前源码同步并重启后，新的 10 分钟窗口仍出现 3 条 `execution_error`，没有 Traceback。
- 已排除：Windows 源码与 WSL 运行副本已重新同步；发布门禁通过；插件加载和 `AstrBot started` 已确认；日志没有图片路径、Base64、文件读取、原生 QQ 表情同步、OneBot 断连或非零 action 结果证据。
- 影响：自动贴图收藏可能失败，当前没有证据表明普通文本回复、容器可用性或 QQ 连接受到影响。
- 候选方向：继续收集安全的异常类型和工具上下文；临时关闭自动贴图收藏；检查或升级 AstrBot 工具循环实现；在根因确认后再增加 `sticker.consider` 的输入/文件异常兜底。
- 需要决策：是否暂时关闭自动贴图收藏，还是先保留功能并优先做 AstrBot/工具循环兼容性调查与诊断增强。
- 状态：待决策。

## 已拍板不改

暂无。
