review 进度：本轮 review 结尾 commit id `9fd8627`

## 当前剩余问题

### [待决策] 修复后 `sticker.consider` 再次出现执行失败

- 发现范围：2026-08-05 00:00:35Z 至 03:30:38Z；本轮增量窗口为 02:30:37Z 至 03:30:38Z。
- 证据：前序窗口共有 4 条 `execution_error`；本轮 1477 条日志行中有 9 条同类 `execution_error`，其中 8 条直接带 `sticker.consider` / `yebot_sticker_consider`，并出现 3 条 Traceback/TypeError。脱敏调用路径聚合到 AstrBot `call_local_llm_tool`、`_execute_local`、`astr_agent_tool_exec` 和 `tool_loop_agent`；TypeError 形态为 2 条 unexpected-keyword mismatch 和 1 条 missing-required-argument mismatch。当前结构指纹为 `0ff9e4999cfaf0e6`（8 次）与 `cfc55fe5a905fbc2`（1 次）。
- 已排除：没有图片源不可用、Base64、文件读取、原生表情同步、超时、参数校验、权限或连接失败证据；修复前缺少 `should_collect` 的 TypeError 形态未复现。
- 影响：最近窗口内至少有 8 次贴图相关工具循环失败，自动贴图收录决策可能未完成；当前没有证据表明普通回复、容器可用性或 QQ 链路受到影响。
- 候选方向：检查并升级/修复 AstrBot 本地工具循环适配；暂时关闭 `sticker_auto_collect`；在 YeBot 工具入口增加阶段和异常类型的脱敏诊断后继续观察。
- 需要决策：是否调整 AstrBot 版本或工具循环部署策略，是否暂时关闭自动收录，以及是否接受增加运行诊断日志。
- 最新增量：2026-08-05 04:28:53Z 至 04:33:09Z 采集 205 条日志行，出现 28 条 `sticker.consider` 相关信号和 9 条图片/原生表情错误信号；没有 Traceback、`execution_error`、`status=failed`、Base64、图片源不可用或连接断线信号。伪造聊天记录未再次出现执行错误。
- 最新增量：2026-08-05 04:33:09Z 至 05:13:48Z 的脱敏聚合出现 2 次独立 `TypeError`、1 次贴图工具 `execution_error` 和 1 次未关联 action 的 `ActionFailed`（`retcode=1200`）；转发场景在 04:44:25Z 完成 `send_group_forward_msg` 且返回成功。没有证据把 `retcode=1200` 归因到转发，继续按贴图问题待决策，不修改业务代码。
- 最新增量：2026-08-05 05:17:29Z 至 05:35:26Z 采集 204 条日志行，去重后出现 8 个贴图工具阶段、2 次 `execution_error`、1 次 Traceback 和 1 个时间点的 `ActionFailed`（其中 2 条记录带 `retcode=1200`）；没有 `send_group_forward_msg`、容器退出、明确拒绝连接或断线信号。根因仍未确认，继续按贴图问题待决策，不修改业务代码。
- 最新增量：2026-08-05 05:37:10Z 至 06:04:07Z 采集 688 条日志行，去重后出现 11 个 `yebot_sticker_consider` 阶段、1 次 Traceback 和 1 个未关联 action 的 `ActionFailed`（`retcode=1200`）；没有 `execution_error`、`send_group_forward_msg`、容器退出、明确拒绝连接或断线信号。根因仍未确认，继续按贴图问题待决策，不修改业务代码。
- 最新增量：2026-08-05 06:05:09Z 至 06:34:14Z 采集 748 条日志行，去重后出现 26 个 `yebot_sticker_consider` 阶段和 2 次 `execution_error`；没有 Traceback、`ActionFailed`、`send_group_forward_msg`、容器退出、明确拒绝连接或断线信号。根因仍未确认，继续按贴图问题待决策，不修改业务代码。
- 最新增量：2026-08-05 06:35:20Z 至 07:04:39Z 采集 933 条日志行，出现 5 次贴图工具 `execution_error`、1 次 Traceback 和 1 次未关联 action 的 `ActionFailed`（`retcode=1200`）。转发工具有 1 次前置阶段失败但未发出 action，随后在 06:37:13Z 成功完成 `send_group_forward_msg`；没有转发 action 失败、容器退出或明确断线信号。前置失败根因未确认，继续按待决策问题记录，不修改业务代码。
- 最新增量：2026-08-05 07:05:53Z 至 07:34:05Z 采集 738 条日志行，去重后出现 24 个 `yebot_sticker_consider` 阶段和 1 个未关联 action 的 `ActionFailed`（`retcode=1200`）；没有 Traceback、`execution_error`、`send_group_forward_msg`、容器退出、明确拒绝连接或断线信号。根因仍未确认，继续按贴图问题待决策，不修改业务代码。
- 最新增量：2026-08-05 08:03:44Z 至 08:34:54Z 采集 522 条日志行，去重后出现 16 个 `yebot_sticker_consider` 阶段、1 次 `execution_error` 和 1 个未关联 action 的 `ActionFailed`（`retcode=1200`）；没有 Traceback、`send_group_forward_msg`、容器退出、明确拒绝连接或断线信号。根因仍未确认，继续按贴图问题待决策，不修改业务代码。
- 最新增量：2026-08-05 08:36:04Z 至 09:03:13.530Z 采集 327 条日志行（`astrbot` 220、`napcat` 107），出现 72 个 `yebot_sticker_consider` 阶段、22 个 `sticker.consider` 阶段、1 次贴图相关 `execution_error` 和 1 个未关联 action 的 `ActionFailed`（`retcode=1200`）；没有 Traceback、`TypeError`、图片源/Base64 错误、`send_group_forward_msg`、容器退出或明确拒绝连接/断线信号。根因仍未确认，继续按贴图问题待决策，不修改业务代码。
- 最新增量：2026-08-05 09:03:13.530Z 至 09:33:14.033Z 采集 905 条日志行（`astrbot` 618、`napcat` 287），出现 61 个 `yebot_sticker_consider` 阶段、20 个 `sticker.consider` 阶段、3 组 `Traceback/TypeError` 和 2 次 `execution_error`；脱敏调用均聚合到 AstrBot `tool_loop_agent`，其中 1 组为 unexpected-keyword 形态。没有 `ActionFailed`、`send_group_forward_msg`、图片源/Base64 错误、容器退出或明确连接失败信号。根因仍未确认，继续按贴图问题待决策，不修改业务代码。
- 最新增量：2026-08-05 11:23:15.678Z 至 11:56:14.669Z 采集 1244 条日志行，出现 3 次带 `sticker.consider`/`yebot_sticker_consider` 标记的 `execution_error`；另有 8 次 Traceback，异常类型为 `ClientConnectorDNSError`/`gaierror`，调用栈聚合到 AstrBot `tool_loop_agent` 的标准地址解析阶段，归入新增的外部 DNS 问题，不据此扩大贴图问题结论。没有 YeBot 导入失败、TypeError、容器退出或 OneBot 断线信号。
- 最新增量：2026-08-05 11:56:14.669Z 至 12:24:43.957Z 采集 706 条日志行，出现 2 次带 `sticker.consider`/`yebot_sticker_consider` 标记的 `execution_error`、4 次与贴图工具循环相邻的 Traceback 和 16 次 `ActionFailed`；没有 TypeError、DNS 解析异常、导入失败、容器退出或 OneBot 断线信号，继续按贴图问题待决策。
- 最新增量：2026-08-05 12:24:43.957Z 至 12:54:19.352Z 采集 1512 条日志行，出现 1 次带 `sticker.consider`/`yebot_sticker_consider` 标记的 `execution_error`、10 次与贴图工具循环相邻的 Traceback 和 41 次 `ActionFailed`；没有 TypeError、DNS 解析异常、导入失败、容器退出或 OneBot 断线信号，继续按贴图问题待决策。
- 最新增量：2026-08-05 12:54:19.352Z 至 13:24:05.131Z 采集 1679 条日志行，出现 5 次带 `sticker.consider`/`yebot_sticker_consider` 标记的 `execution_error`、11 次与贴图工具循环相邻的 Traceback 和 45 次 `ActionFailed`；没有 TypeError、DNS 解析异常、导入失败、容器退出或 OneBot 断线信号，继续按贴图问题待决策。
- 最新增量：2026-08-05 13:24:05.131Z 至 13:55:47.027Z 采集 1650 条日志行，重启前出现 10 次与贴图工具循环相邻的 Traceback 和 40 次 `ActionFailed`，没有 execution_error、TypeError、DNS 解析异常、导入失败或断线信号；13:48:07Z 两个容器同时启动，重启后 230 条日志无 Traceback/execution_error，继续按贴图问题待决策。
- 最新增量：2026-08-06 01:54:07.381Z 至 02:24:07.813Z 采集 846 条日志行（`astrbot` 303、`napcat` 543），出现 6 个贴图阶段但没有贴图执行错误；3 次 `yebot_message_send` 的 `execution_error` 与 5 次 AstrBot `tool_loop_agent` Traceback 另记入工具循环/DNS 待确认问题。
- 最新增量：2026-08-06 14:39:33Z 至 15:09:34Z 采集 398 条日志行（`astrbot` 271、`napcat` 127），出现 23 条 AstrBot `tool_loop_agent` 错误级信号，其中 14 条带 `sticker.consider`/`yebot_sticker_consider` 标记；没有 Traceback、`execution_error`、TypeError、导入失败、连接/DNS/TTS 失败。另有 1 次未关联 action 的 `ActionFailed`（`retcode=1200`），未归因到贴图或转发。根因仍未确认，继续按贴图工具循环问题待决策，不修改业务代码。
- 最新增量：2026-08-06 15:18:34Z 至 15:39:34Z 采集 77 条日志行（`astrbot` 52、`napcat` 25），出现 10 个贴图阶段和 3 条 AstrBot `tool_loop_agent` 错误级信号，其中 3 条带贴图错误标记；没有 Traceback、`execution_error`、TypeError、导入失败、连接/DNS/TTS 失败或 `ActionFailed`。根因仍未确认，继续按贴图工具循环问题待决策，不修改业务代码。
- 最新增量：2026-08-06 15:41:55Z 至 16:09:35Z 采集 347 条日志行（`astrbot` 246、`napcat` 101），出现 65 个贴图阶段、23 条 AstrBot `tool_loop_agent` 错误级信号和 3 条带贴图标记的 `execution_error`；没有 Traceback、TypeError、导入失败、连接/DNS/TTS 失败、`ActionFailed` 或 `system_info` 相关错误。根因仍未确认，继续按贴图工具循环问题待决策，不修改业务代码。
- 最新增量：2026-08-06 16:12:03Z 至 16:39:35Z 采集 180 条日志行（`astrbot` 131、`napcat` 49），出现 46 个贴图阶段和 15 条 AstrBot `tool_loop_agent` 错误级信号，均带贴图错误标记；没有 Traceback、`execution_error`、TypeError、导入失败、连接/DNS/TTS 失败或 `ActionFailed`。根因仍未确认，继续按贴图工具循环问题待决策，不修改业务代码。
- 最新增量：2026-08-06 16:41:32Z 至 17:09:36Z 采集 227 条日志行（`astrbot` 161、`napcat` 66），出现 55 个贴图阶段、19 条 AstrBot `tool_loop_agent` 错误级信号和 4 条带贴图标记的 `execution_error`；没有 Traceback、TypeError、导入失败、连接/DNS/TTS 失败或 `ActionFailed`。根因仍未确认，继续按贴图工具循环问题待决策，不修改业务代码。
- 最新增量：2026-08-06 17:11:15Z 至 17:39:36Z 采集 434 条日志行（`astrbot` 325、`napcat` 110），出现 51 个贴图阶段、21 条 AstrBot `tool_loop_agent` 错误级信号和 17 条贴图错误级信号；没有 Traceback、`execution_error`、TypeError、可确认的导入失败、连接/DNS/TTS 失败或 `ActionFailed`。期间 AstrBot 于 17:28:33Z 单独重启，重启计数为 0 且重启后无异常；Windows 当前工具文件与 WSL 运行副本哈希不一致，因工作区存在并行未提交改动，本轮不执行同步或覆盖。根因仍未确认，继续按贴图工具循环问题待决策。
- 最新增量：2026-08-06 19:10:25Z 至 19:39:38Z 采集 28 条日志行（`astrbot` 23、`napcat` 5），出现 12 个贴图阶段和 4 条 AstrBot `tool_loop_agent` 错误级信号；没有 Traceback、`execution_error`、TypeError、导入失败、连接/DNS/TTS 失败、`ActionFailed` 或 `system_info` 异常。根因仍未确认，继续按贴图工具循环问题待决策，不修改业务代码。
- 最新增量：2026-08-06 23:09:40Z 至 23:39:41Z 采集 21 条日志行（`astrbot` 15、`napcat` 6），出现 6 个 `yebot_sticker_consider`/贴图工具循环信号和 2 条 AstrBot 工具循环错误级信号；另有 1 条 warning。没有 `execution_error`、Traceback、TypeError、导入失败、连接/DNS/TTS 失败或 `ActionFailed`。根因仍未确认，继续按贴图工具循环问题待决策，不修改业务代码。
- 最新增量：2026-08-06 23:39:41Z 至 2026-08-07 00:09:41Z 采集 56 条日志行（`astrbot` 40、`napcat` 16），出现 9 个 `yebot_sticker_consider`/工具循环信号和 3 条 AstrBot 工具循环错误级信号；另有 7 条 provider/model warning，未包含失败或异常标记。没有 `execution_error`、Traceback、TypeError、导入失败、连接/DNS/TTS 失败或 `ActionFailed`。根因仍未确认，继续按贴图工具循环问题待决策，不修改业务代码。
- 最新增量：2026-08-07 00:09:41Z 至 00:39:42Z 采集 320 条日志行（`astrbot` 230、`napcat` 90），出现 40 个贴图阶段、59 条 AstrBot `tool_loop_agent` 信号和 4 条 `execution_error`；4 条均关联 `sticker.consider`/`yebot_sticker_consider`。没有 Traceback、TypeError、导入失败、连接/DNS/TTS 失败、非贴图 `execution_error` 或 `ActionFailed`；另有 19 条 provider/model warning，未包含失败或异常标记。根因仍未确认，继续按贴图工具循环问题待决策，不修改业务代码。
- 最新增量：2026-08-07 00:39:42Z 至 01:09:42Z 采集 267 条日志行（`astrbot` 206、`napcat` 61），出现 33 个 `yebot_sticker_consider`/贴图阶段和 70 条 AstrBot `tool_loop_agent` 信号；其中 20 条为错误级信号，17 条带贴图标记。没有 `execution_error`、Traceback、TypeError、导入失败、连接/DNS/TTS 失败或 `ActionFailed`；另有 14 条 provider/model warning，未包含失败或异常标记。根因仍未确认，继续按贴图工具循环问题待决策，不修改业务代码。
- 最新增量：2026-08-07 01:09:42Z 至 01:39:43Z 采集 1126 条日志行（`astrbot` 812、`napcat` 314），出现 88 个贴图阶段、165 条 AstrBot `tool_loop_agent` 信号和 9 条贴图相关 `execution_error`；另有 1 次 Traceback、16 条 `BadRequestError` 日志行、5 条 `server_error` 和 54 条 warning，其中 45 条带 provider/model 标记。没有 TypeError、导入失败、连接/DNS/TTS 失败、非贴图 `execution_error` 或 `ActionFailed`。贴图循环与 Provider 异常的因果关系仍未确认，继续分别按待决策问题记录。
- 最新增量：2026-08-07 01:39:43Z 至 02:09:43Z 采集 724 条日志行（`astrbot` 529、`napcat` 195），出现 160 个贴图阶段、215 条 AstrBot `tool_loop_agent` 信号和 6 条贴图相关 `execution_error`；另有 1 个未关联 action 的 `ActionFailed`（`retcode=1200`）。没有 Traceback、TypeError、Provider `BadRequestError`/`server_error`、导入失败、DNS/TTS 失败或非贴图 `execution_error`；55 条 warning 中 41 条带 provider/model 标记，未包含失败或异常标记。根因仍未确认，继续按贴图工具循环问题待决策，不修改业务代码。
- 最新增量：2026-08-07 02:09:43Z 至 02:39:43Z 采集 1217 条日志行（`astrbot` 820、`napcat` 397），出现 88 个贴图阶段、131 条 AstrBot `tool_loop_agent` 信号和 4 条贴图相关 `execution_error`；另有 1 次 Traceback，异常类型包含 `EmptyModelOutputError` 和 `RetryError`，并出现 1 次 `ProviderOpenAIOfficial._handle_api_error`。没有 TypeError、`BadRequestError`、`server_error`、导入失败、DNS/TTS 失败、非贴图 `execution_error` 或 `ActionFailed`；44 条 warning 中 38 条带 provider/model 标记。贴图循环与 Provider 异常的因果关系仍未确认，继续分别按待决策问题记录。
- 最新增量：2026-08-07 02:39:43.940Z 至 03:09:44.380Z 采集 515 条日志行（`astrbot` 323、`napcat` 192），出现 40 个贴图阶段、42 条 AstrBot `tool_loop_agent` 信号和 2 条 `execution_error`；2 条均关联 `sticker.consider`/`yebot_sticker_consider`。没有 Traceback、TypeError、Provider 异常、导入失败、连接/DNS/TTS 失败、非贴图 `execution_error` 或 `ActionFailed`；两个容器均为 running，`RestartCount=0`、`OOMKilled=false`。根因仍未确认，继续按贴图工具循环问题待决策，不修改业务代码或运行配置。
- 最新增量：2026-08-07 03:09:44.380Z 至 03:39:44.885Z 采集 869 条日志行（`astrbot` 552、`napcat` 317），出现 82 个贴图阶段、91 条 AstrBot `tool_loop_agent` 信号和 2 条 `execution_error`；2 条均关联 `sticker.consider`/`yebot_sticker_consider`。没有 Traceback、TypeError、Provider 异常、导入失败、连接/DNS/TTS 失败、非贴图 `execution_error` 或 `ActionFailed`；两个容器均为 running，`RestartCount=0`、`OOMKilled=false`。根因仍未确认，继续按贴图工具循环问题待决策，不修改业务代码或运行配置。
- 最新增量：2026-08-07 03:39:44.885Z 至 04:09:45.346Z 采集 568 条日志行（`astrbot` 388、`napcat` 180），出现 102 个贴图阶段、105 条 AstrBot `tool_loop_agent` 信号和 6 条 `execution_error`；6 条均关联 `sticker.consider`/`yebot_sticker_consider`。没有 Traceback、TypeError、Provider 异常、导入失败、连接/DNS/TTS 失败、非贴图 `execution_error` 或 `ActionFailed`；两个容器均为 running，`RestartCount=0`、`OOMKilled=false`。根因仍未确认，继续按贴图工具循环问题待决策，不修改业务代码或运行配置。
- 最新增量：2026-08-07 04:09:45.346Z 至 04:39:45.749Z 采集 939 条日志行（`astrbot` 655、`napcat` 284），出现 125 个贴图阶段、155 条 AstrBot `tool_loop_agent` 信号和 7 条 `execution_error`；7 条均关联 `sticker.consider`/`yebot_sticker_consider`。没有 Traceback、TypeError、Provider 异常、导入失败、连接/DNS/TTS 失败、非贴图 `execution_error` 或 `ActionFailed`；两个容器均为 running，`RestartCount=0`、`OOMKilled=false`。根因仍未确认，继续按贴图工具循环问题待决策，不修改业务代码或运行配置。
- 最新增量：2026-08-07 04:39:45.749Z 至 05:09:46.141Z 采集 1207 条日志行（`astrbot` 795、`napcat` 412），出现 178 个贴图阶段、184 条 AstrBot `tool_loop_agent` 信号和 5 条 `execution_error`；5 条均关联 `sticker.consider`/`yebot_sticker_consider`。没有 Traceback、TypeError、Provider 异常、导入失败、连接/DNS/TTS 失败、非贴图 `execution_error` 或 `ActionFailed`；两个容器均为 running，`RestartCount=0`、`OOMKilled=false`。根因仍未确认，继续按贴图工具循环问题待决策，不修改业务代码或运行配置。
- 最新增量：2026-08-07 05:09:46.141Z 至 05:39:46.566Z 采集 603 条日志行（`astrbot` 408、`napcat` 195），出现 84 个贴图阶段、95 条 AstrBot `tool_loop_agent` 信号和 6 条 `execution_error`；6 条均关联 `sticker.consider`/`yebot_sticker_consider`。没有 Traceback、TypeError、Provider 异常、导入失败、连接/DNS/TTS 失败、非贴图 `execution_error` 或 `ActionFailed`；两个容器均为 running，`RestartCount=0`、`OOMKilled=false`。根因仍未确认，继续按贴图工具循环问题待决策，不修改业务代码或运行配置。
- 最新增量：2026-08-07 05:39:46.566Z 至 06:09:46.975Z 采集 770 条日志行（`astrbot` 544、`napcat` 226），出现 123 个贴图阶段、159 条 AstrBot `tool_loop_agent` 信号和 4 条贴图相关 `execution_error`；另有 2 组 Traceback/TypeError，异常形态均为 `unexpected-keyword`，其中 1 组带贴图标记、1 组没有贴图标记。Windows 与 WSL 的插件关键文件哈希一致；没有 Provider、DNS/TTS、导入、连接或 `ActionFailed` 信号。贴图问题继续待决策，未关联贴图的工具循环异常单独待确认。
- 最新增量：2026-08-07 06:09:46.975Z 至 07:09:47.873Z 采集 529 条日志行（`astrbot` 344、`napcat` 187），出现 73 个贴图阶段、76 条 AstrBot `tool_loop_agent` 信号和 2 条贴图相关 `execution_error`；没有 Traceback、TypeError、Provider 异常、导入失败、连接/DNS/TTS 失败、非贴图 `execution_error` 或 `ActionFailed`；两个容器均为 running，`RestartCount=0`、`OOMKilled=false`。根因仍未确认，继续按贴图工具循环问题待决策，不修改业务代码或运行配置。
- 最新增量：2026-08-07 07:09:47.873Z 至 07:39:48.332Z 采集 566 条日志行（`astrbot` 345、`napcat` 223），出现 27 个贴图阶段、39 条 AstrBot `tool_loop_agent` 信号和 0 条 `execution_error`；另有 3 条未关联贴图的图片功能错误、1 次未关联 action 的 `ActionFailed`（`retcode=1200`）和 1 条未表现为断线的 WebSocket 状态标记。没有 Traceback、TypeError、Provider、DNS/TTS、导入失败或非贴图 `execution_error`；根因仍未确认，继续观察，不修改业务代码或运行配置。
- 最新增量：2026-08-07 07:39:48.332Z 至 08:14:48.790Z 采集 1068 条日志行（`astrbot` 719、`napcat` 349），出现 6 条贴图相关 `execution_error`、1 组未关联贴图的 `Traceback/TypeError` 和 1 次未关联 action 的 `ActionFailed`；另有 6 条 `yebot_message_send` 信号与 2 次 `send_group_msg`，其中 1 次发送链路带普通 `at` 文本标记但没有 `CQ:at` 或结构化 `type=at`。没有 Provider、DNS/TTS、导入失败或断线信号；贴图异常继续待决策，At 发送问题单独记录。
- 最新增量：2026-08-07 08:14:48.790Z 至 08:44:49.326Z 采集 761 条日志行（`astrbot` 521、`napcat` 240），出现 172 条 AstrBot `tool_loop_agent` 信号和 3 条贴图相关 `execution_error`；另有 2 次 `send_group_msg`，没有 `yebot_message_send`、At 结构化段、Traceback、TypeError、Provider、DNS/TTS、导入失败、ActionFailed 或连接断线信号。At 问题本窗口未复现，继续按待决策问题观察。
- 最新增量：2026-08-07 09:44:50.171Z 至 10:14:50.640Z 采集 4973 条脱敏日志行（`astrbot` 4877、`napcat` 96），出现 75 条 `tool_loop_agent`、20 条含 `Traceback` 的日志行和 4 条未处理异常标记；没有 `execution_error`、TypeError、导入失败、连接失败、断线或 `ActionFailed`。另有 31 条 Provider/模型错误信号和 14 条限流/额度信号，根因继续待确认。
- 最新增量：2026-08-07 10:14:50.640Z 至 10:45:06.449Z 采集 2062 条脱敏日志行（`astrbot` 1885、`napcat` 176），出现 84 条 `tool_loop_agent` 和 8 条贴图相关 `execution_error`；没有 Traceback、未处理异常、TypeError、导入失败、连接失败、断线或 `ActionFailed`。另有 17 条 Provider/模型错误信号和 22 条限流/额度信号，继续按既有待确认问题观察。
- 状态：待决策。

### [待确认/待决策] 普通消息发送中的 at 未转换为真实 QQ At

- 发现范围：2026-08-07T07:39:48.332Z 至 2026-08-07T08:14:48.790Z。
- 证据：`16:04:59.303` 的 `yebot_message_send` 与 `16:04:59.590` 的 `send_group_msg` 成对出现；两条信号均有普通 `at` 文本标记，没有 `CQ:at`、OneBot `type=at` 或可确认的目标字段。窗口内唯一 `CQ:at` 信号没有关联发送 action。
- 技术根因：`main.py` 的 `yebot_message_send` 参数是普通字符串；`yebot/runtime/tools/onebot.py` 的 `send_message` 强制要求字符串并原样传给 `send_group_msg`，没有结构化消息段或目标解析。提醒确认路径也会把已解析目标拼成 `Plain("@QQ")`。
- 影响：需要提醒指定成员时，用户可能看到 `at`/`@QQ` 文本，QQ 客户端没有产生真实提醒；当前证据只覆盖这次发送，未扩大到所有普通回复。
- 候选方向：
  1. 为普通发送工具增加显式目标参数，先用当前群成员/回复/当前事件解析目标，再构造 OneBot `at` 段并发送。
  2. 仅修复提醒确认路径，使用 AstrBot `At` 组件或结构化 OneBot 段；普通消息工具继续只发送文本。
  3. 允许解析 `@数字` 或其他自然语言目标，但需要明确误触发、跨群目标和权限校验规则。
- 需要决策：普通 `yebot_message_send` 是否支持真实 At；目标是否必须来自当前群解析结果；是否接受调整工具参数契约和模型调用方式。根因已确认，修复范围涉及产品语义与目标权限，当前暂停代码修改。
- 后续复核：2026-08-07 09:44:50.171Z 至 10:14:50.640Z 出现 16 条 `yebot_message_send` 和 3 条 `send_group_msg` 信号；可见 At 标记没有伴随 `CQ:at` 或结构化 `type=at`，但脱敏日志包含完整上下文，无法确认标记属于出站 `message` 参数。
- 后续复核：2026-08-07 10:14:50.640Z 至 10:45:06.449Z 出现 18 条 `yebot_message_send` 和 2 条 `send_group_msg` 信号，仍没有 `CQ:at` 或结构化 `type=at`；At 语义和目标解析边界保持待决策。
- 状态：待确认/待决策。

### [待确认/待决策] AstrBot 工具循环 unexpected-keyword TypeError

- 发现范围：2026-08-07 05:39:46.566Z 至 06:09:46.975Z。
- 证据：窗口 770 条脱敏日志中有 2 组 Traceback/TypeError，异常形态均为 `unexpected-keyword`，两组均聚合到 AstrBot `tool_loop_agent`；其中 1 组带 `sticker.consider`/`yebot_sticker_consider`，另 1 组没有贴图标记，结构指纹记录在本轮单次 review 中。窗口没有 Provider、DNS/TTS、导入、连接或 `ActionFailed` 信号。
- 根因边界：Windows 与 WSL 运行副本的 `main.py`、`yebot/runtime/tools/catalog.py` SHA-256 一致；当前 YeBot 贴图工具签名包含默认参数，暂未发现插件副本过期或本地参数定义不一致证据。调用栈信号集中在 AstrBot 工具循环，外部适配契约或版本行为仍待确认。
- 影响：至少 1 个未关联贴图的工具循环调用可能未完成；普通回复、容器可用性和 OneBot 主链路当前没有受影响证据。
- 候选方向：核对 AstrBot `call_local_llm_tool`/`_execute_local` 的参数契约、版本和运行配置；确认后再决定是否调整 AstrBot 部署或工具循环适配；保持诊断脱敏。
- 需要决策：是否允许检查或调整 AstrBot 工具循环代码、版本和部署策略；是否接受工具调用契约或失败重试行为变化。
- 后续复核：2026-08-07 10:14:50.640Z 至 10:45:06.449Z 出现 8 条 `execution_error`，均带贴图流程标记；未见新的未关联 TypeError 或导入/连接失败证据。
- 状态：待确认/待决策。

### [待确认/待决策] AstrBot 外部模型 Provider 请求失败

- 发现范围：2026-08-07 01:09:42.562Z 至 01:39:43.055Z。
- 证据：1126 条脱敏日志中出现 16 条 `BadRequestError` 日志行、5 条 `server_error` 和 1 个 Traceback；调用路径出现 AstrBot `ProviderOpenAIOfficial._handle_api_error`。两类异常的直接日志行没有贴图标记；同窗口的 9 条贴图 `execution_error` 单独记录，因果关系未确认。
- 影响：部分外部模型调用可能未完成；容器和 OneBot 主链路仍在线，暂未发现 YeBot 导入或运行副作用。
- 候选方向：核对 Provider 请求格式、模型能力、上游状态和配置；确认后再决定是否调整 Provider、AstrBot 版本或重试/降级策略，并保持错误诊断脱敏。
- 需要决策：是否允许检查或调整外部 Provider 配置、账号/凭据和部署策略；是否接受 Provider 重试或降级行为变化。
- 后续复核：2026-08-07 02:09:43.460Z 至 02:39:43.940Z 出现 1 次 Traceback，脱敏异常类型包含 `EmptyModelOutputError` 和 `RetryError`，并出现 1 次 `ProviderOpenAIOfficial._handle_api_error`；异常直接日志没有贴图标记。当前仍无法区分 Provider 请求契约、模型能力、配置、凭据、重试耗尽或上游波动。
- 后续复核：2026-08-07 09:44:50.171Z 至 10:14:50.640Z 出现 20 条含 `Traceback` 的日志行、4 条未处理异常标记、31 条 Provider/模型错误信号和 14 条限流/额度信号；脱敏异常类型候选包含 `HTTPStatusError`、`BrokenResourceError`、`ReadError`、`TimeoutError`，没有 YeBot 文件栈或 OneBot 断线证据。
- 后续复核：2026-08-07 10:14:50.640Z 至 10:45:06.449Z 出现 17 条 Provider/模型错误信号和 22 条限流/额度信号，没有 Traceback 或未处理异常；请求契约、模型能力、配置、凭据与上游波动仍无法区分。
- 状态：待确认/待决策。

### [待确认/待决策] AstrBot 工具循环外部 DNS 解析失败

- 发现范围：2026-08-05 11:23:15.678Z 至 11:56:14.669Z。
- 证据：1244 条脱敏日志中出现 8 次 Traceback，异常类型为 `ClientConnectorDNSError` 2 次和 `gaierror` 1 次；调用栈聚合到 `getaddrinfo`/`resolve` 与 AstrBot `tool_loop_agent`，没有 YeBot 文件栈、导入失败或 OneBot 断线信号。
- 影响：个别外部模型工具调用可能因地址解析失败而未完成；容器和主链路仍在线，根因暂不能区分 WSL/Docker DNS、上游服务波动或 AstrBot 网络配置。
- 候选方向：检查容器 DNS 与上游服务可达性；确认后再决定是否调整部署配置、AstrBot 网络重试或降级策略。
- 需要决策：是否允许调整 WSL/Docker DNS、上游服务配置或 AstrBot 网络重试；根因确认前暂停 YeBot 业务代码修改。
- 后续复核：2026-08-05 11:56:14.669Z 至 12:24:43.957Z 未出现 `ClientConnectorDNSError`/`gaierror`，容器和连接状态正常；该问题暂不关闭，继续观察是否重复。
- 后续复核：2026-08-06 01:54:07.381Z 至 02:24:07.813Z 采集 846 条日志行，出现 5 次 AstrBot `tool_loop_agent` Traceback 和 3 次 `yebot_message_send` `execution_error`；其中 1 次带 DNS/地址解析信号，其他根因未确认。02:19:33.548581Z 出现反向 WebSocket/OneBot 建连标记，窗口余下时间没有新的工具失败或连接断线信号，继续暂停 YeBot 业务代码修改。
- 状态：待确认/待决策。

### [待确认] AstrBot 与 NapCat 同时外部重启

- 发现范围：2026-08-05 13:24:05.131Z 至 13:55:47.027Z。
- 证据：两个容器 `StartedAt` 均为 2026-08-05T13:48:07Z，`RestartCount=0`；没有可归因的 Docker Compose 事件记录。重启后 230 条日志包含 37 个插件加载信号，无 Traceback、execution_error、导入失败或连接断线。
- 影响：存在一次短暂服务中断或连接重建风险，当前没有容器崩溃或 YeBot 代码故障证据。
- 候选方向：核对计划内运维/主机记录；若无记录，继续观察并调查 Docker/WSL 或部署自动化。
- 需要决策：确认该重启是否计划内；若不是，是否允许继续调查主机与部署自动化。
- 状态：待确认。


## 已拍板不改

暂无。
